{
  "name": "OpenBrigade Dev Container",
  "workspaceFolder": "/workspace",

  "features": {
    "ghcr.io/devcontainers/features/git:1": {},
    "ghcr.io/devcontainers/features/docker-in-docker:2": {
      "moby": false
    },
    "ghcr.io/devcontainers/features/php:1": {
      "version": "8.4",
      "extensions": [
        "intelephense",
        "xdebug",
        "twig",
        "phpmd"
      ]
    }
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "GitHub.copilot-chat",
        "bmewburn.vscode-intelephense-client",
        "xdebug.php-debug",
        "mblode.twig-language-2",
        "ecodes.vscode-phpmd",
        "streetsidesoftware.code-spell-checker",
        "ms-azuretools.vscode-docker",
        "mtxr.sqltools",
        "mtxr.sqltools-driver-mysql",
        "github.vscode-github-actions"
      ],
      "settings": {
        "php.validate.executablePath": "/usr/local/bin/php",
        "intelephense.environment.phpVersion": "8.4.0",
        "editor.formatOnSave": false,
        "files.eol": "\n"
      }
    }
  },

  "initializeCommand": "mkdir -p /workspace && test -f .env || cp .env.example.dev .env",
  "postCreateCommand": "echo '\\n=== OpenBrigade Development Environment ===' && echo '\\nTo start the Docker Compose services, run:' && echo '\\n  docker-compose up -d\\n' && echo 'Services:' && [...]",
  "forwardPorts": [8080, 8081],
  "portsAttributes": {
    "8080": { "label": "OpenBrigade App", "onAutoForward": "notify" },
    "8081": { "label": "CloudBeaver", "onAutoForward": "notify" },
    "db:3306": { "label": "MySQL / MariaDB" }
  },

  "remoteUser": "root"
}
