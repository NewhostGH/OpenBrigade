# OpenBrigade environment variables
# Copy this file to .env and edit as needed before running `docker compose up`
# Run `php artisan key:generate` on first install to set APP_KEY.

APP_NAME=OpenBrigade
APP_ENV=local
APP_KEY=   # Run: php artisan key:generate --show  and paste the output here
APP_DEBUG=true
APP_URL=http://localhost:8080
#TRUST_PROXIES=*
APP_TIMEZONE=Europe/Paris
APP_LOCALE=fr

# Application port on the host
APP_PORT=8080

# phpMyAdmin port on the host
PMA_PORT=8081

# Database settings
DB_CONNECTION=mariadb
DB_HOST=db
DB_PORT=3306
DB_NAME=openbrigade
DB_USER=openbrigade
DB_DATABASE=openbrigade
DB_USERNAME=openbrigade
DB_PASSWORD=CHANGE_ME
DB_ROOT_PASSWORD=CHANGE_ME_ROOT
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci

# Upload limits (must match php.ini values)
MAX_UPLOAD_MB=20
MAX_LIST_ROWS=500

# Logging
LOG_CHANNEL=stack
LOG_LEVEL=debug
